<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.example</groupId>
    <artifactId>apigee-design-automation</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <!-- Use an earlier version of the plugin that's more readily available -->
        <apigee.config.plugin.version>1.4.3</apigee.config.plugin.version>
        <!-- Credentials will be injected by GitHub Actions environment variables -->
        <apigee.org>${env.APIGEE_ORG}</apigee.org>
        <apigee.env>${env.APIGEE_ENV}</apigee.env>
        <apigee.token>${env.APIGEE_TOKEN}</apigee.token>
    </properties>

    <!-- Add repositories section with Apigee public Maven repo -->
    <repositories>
        <repository>
            <id>central</id>
            <name>Central Repository</name>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
        <repository>
            <id>apigee-public</id>
            <name>Apigee Public Repository</name>
            <url>https://github.com/apigee/apigee-deploy-maven-plugin/raw/mvn-repo</url>
        </repository>
    </repositories>

    <!-- Add pluginRepositories section as well -->
    <pluginRepositories>
        <pluginRepository>
            <id>central</id>
            <name>Central Repository</name>
            <url>https://repo.maven.apache.org/maven2</url>
        </pluginRepository>
        <pluginRepository>
            <id>apigee-public</id>
            <name>Apigee Public Repository</name>
            <url>https://github.com/apigee/apigee-deploy-maven-plugin/raw/mvn-repo</url>
        </pluginRepository>
    </pluginRepositories>

    <dependencies>
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>2.2</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- This plugin runs the custom Java code to parse YAML and generate bundles -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>generate-apigee-bundles-from-yaml</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <mainClass>com.example.DesignParser</mainClass>
                            <cleanupDaemonThreads>false</cleanupDaemonThreads>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Using the properly working version of the Apigee plugin -->
            <plugin>
                <groupId>io.apigee.build-tools.enterprise4g</groupId>
                <artifactId>apigee-edge-maven-plugin</artifactId>
                <version>1.4.3</version>
                <executions>
                    <!-- Deploy SharedFlows FIRST -->
                    <execution>
                        <id>deploy-sharedflows</id>
                        <phase>install</phase>
                        <goals>
                            <goal>deploy</goal>
                        </goals>
                        <configuration>
                            <options>update</options>
                            <!-- Shared flow configuration goes here -->
                            <directory>${project.build.directory}/sharedflows</directory>
                        </configuration>
                    </execution>
                    <!-- Deploy API Proxies SECOND -->
                    <execution>
                        <id>deploy-proxies</id>
                        <phase>install</phase>
                        <goals>
                            <goal>deploy</goal>
                        </goals>
                        <configuration>
                            <options>update</options>
                            <!-- API proxy configuration goes here -->
                            <directory>${project.build.directory}/apiproxies</directory>
                        </configuration>
                    </execution>
                </executions>
                <!-- Common configuration for all executions -->
                <configuration>
                    <skip>false</skip>
                    <options>update</options>
                    <authType>oauth</authType> <!-- Use OAuth token -->
                    <bearerToken>${apigee.token}</bearerToken>
                    <org>${apigee.org}</org>
                    <env>${apigee.env}</env>
                    <apiVersion>v1</apiVersion>
                    <hostUrl>https://apigee.googleapis.com</hostUrl>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>